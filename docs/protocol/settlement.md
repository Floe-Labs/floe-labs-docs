# Settlement

- Minimal proxy per loan (CREATE2) escrows principal + collateral.
- Methods: `repay`, `topUp`, `withdrawInterest`, `liquidate`, `refinance` (if enabled).
- Deterministic flows; events for analytics.
